import { CompleteTodoDirective } from './complete-todo-directive';

describe('CompleteTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new CompleteTodoDirective();
    expect(directive).toBeTruthy();
  });
});
