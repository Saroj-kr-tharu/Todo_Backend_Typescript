import { IsMobileView } from './is-mobile-view';

describe('IsMobileView', () => {
  it('should create an instance', () => {
    const directive = new IsMobileView();
    expect(directive).toBeTruthy();
  });
});
